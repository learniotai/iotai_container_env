name: base
channels:
  - pytorch
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - tensorboard
  - tensorboardx
  - jupyter
  - jupyterlab
  - tqdm
  - pip
  - pylint
  - flake8
  - pip:
    - torchsummary
    - tfds-nightly
# Prefix must match base miniconda installation directory 
prefix: /opt/miniconda
