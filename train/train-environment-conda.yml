name: base
channels:
  - pytorch
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - tensorboard
  - tensorboardx
  - jupyter
  - jupyterlab
  - tqdm
  - pylint
  - flake8
  - pip
  - pip:
    - torchsummary
    - tfds-nightly
    - pycocotools
# Prefix must match base miniconda installation directory 
prefix: /opt/miniconda
